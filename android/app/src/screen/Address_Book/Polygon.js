import { View, Text } from 'react-native'
import React from 'react'

const Polygon = (props) => {
    const { navigation } = props
  return (
    <View>
      <Text style={{color:'black'}}>Polygon</Text>
    </View>
  )
}

export default Polygon