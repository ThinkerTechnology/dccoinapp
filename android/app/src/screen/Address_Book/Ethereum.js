import { View, Text } from 'react-native'
import React from 'react'

const Ethereum = (props) => {
    const { navigation } = props
  return (
    <View>
      <Text style={{color:'black'}}>Ethereum</Text>
    </View>
  )
}

export default Ethereum