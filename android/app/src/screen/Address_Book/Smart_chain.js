import { View, Text } from 'react-native'
import React from 'react'

const Smart_chain = (props) => {
    const { navigation } = props
  return (
    <View>
      <Text style={{color:'black'}}>Smart_chain</Text>
    </View>
  )
}

export default Smart_chain