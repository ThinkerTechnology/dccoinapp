import { View, Text } from 'react-native'
import React from 'react'

const Avalanche = (props) => {
    const { navigation } = props
  return (
    <View>
      <Text style={{color:'black'}}>Avalanche</Text>
    </View>
  )
}

export default Avalanche