import { View, Text } from 'react-native'
import React from 'react'

const Aptos = (props) => {
    const { navigation } = props
  return (
    <View>
      <Text style={{color:'black'}}>Aptos</Text>
    </View>
  )
}

export default Aptos